$(document).on('submit', '#studentEntryForm', function(formEvent) {

    //formEvent.preventDefault();

    //var form = $('#modalForm');
    //var button_value = $('#modal_submit_button').attr('value');
    $("#progress-bar").removeClass('hide');

});
